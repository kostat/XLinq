<Project>

    <Target Name="Obfuscar">
        <Exec Command="$(SolutionDir).tools/obfuscar.console obfuscar.xml" WorkingDirectory="$(OutputPath)"/>
        <Copy SourceFiles="$(OutputPath)/obfuscated/$(AssemblyName).dll" DestinationFolder="$(OutputPath)" />
    </Target>

    <Target Name="RunObfuscar" BeforeTargets="pack">
        <CallTarget Targets="Obfuscar" />
    </Target>

    <ItemGroup>
        <None Update="obfuscar.xml">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
        </None>
    </ItemGroup>

</Project>
